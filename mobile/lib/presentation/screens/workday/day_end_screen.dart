import 'package:flutter/material.dart';

class DayEndScreen extends StatelessWidget {
  const DayEndScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('End Day')),
      body: const Center(child: Text('Day End — Implementation in progress')),
    );
  }
}
