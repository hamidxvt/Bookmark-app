import LiveActivityClient from "@/components/live-activity/LiveActivityClient";
export const metadata = { title: "Live Activity — FFM" };
export default function LiveActivityPage() {
  return <LiveActivityClient />;
}
