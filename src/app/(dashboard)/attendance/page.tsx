import AttendanceClient from "@/components/attendance/AttendanceClient";
export const metadata = { title: "Attendance — FFM" };
export default function AttendancePage() {
  return <AttendanceClient />;
}
