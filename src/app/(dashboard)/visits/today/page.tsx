import TodayVisitsClient from "@/components/visits/TodayVisitsClient";

export const metadata = { title: "Today's Visits — FFM" };

export default function TodaysVisitsPage() {
  return <TodayVisitsClient />;
}
